#include <iostream>

using std::cout;

class observer
{
private:
    /* data */
    
public:
    observer(/* args */);
    ~observer();
};

observer::observer(/* args */)
{
}

observer::~observer()
{
}
